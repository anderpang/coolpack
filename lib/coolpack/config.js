
const config={
     /*
    context:"",
    outputPath:"./build",    
       
    templatePath:"",
    module:{
        list:[{
            name:"libs/jquery/2.1.4/jquery.js",
            alias:"jquery",
            untreated:true
        }],
        output:"common.js",
        module:{
            //list:[{name:"w.js"}],
            list:{name:"entry.js"},
            output:"js/init.js",
            css:"css/style.css",
            template:"public/index.html",
            //inject:"head",//"body"
        }
    },
    cssFileLimit:8192,
    rules:{
        js:[],
        css:[],
        template:[]
    },
    babel:{
       plugins:[],
       presets:[]
    },

    uglifyJs:{},
    postcssPlugins:[],
    cssSyntax:"",
    htmlMinify:{
        removeComments:true,
        collapseWhitespace:true
    },

    hash:function(){
        var date=new Date();
        var month=date.getMonth()+1;
        var dt=date.getDate();
        var order=Math.floor(Math.random()*90000)+10000;  
        
        return date.getFullYear()+"-"+(month<10?"0"+month:month)+"-"+(dt<10?"0"+dt:dt)+"-"+order;
    },
    proxy:{},
    
    useStrict:true,
    extractFirstCss:false,
    moduleExtname:[".js"],
    cssExtname:[".css"],
    watch:false,
    watchOptions:{}
    */
};

module.exports=config;